apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: apache-claim0
  name: apache-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
